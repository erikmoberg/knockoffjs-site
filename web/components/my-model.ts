import { SlideModel } from "../../core/models/slide-model";

export class MyModel {
    name: string;
    slides: SlideModel[];
    selectedSlideText: string;
    todoText: string;
    todos: string[];
}